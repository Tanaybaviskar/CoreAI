# The Node/Express proxy (for chat, dashboard, memory, activity, tasks)
NEXT_PUBLIC_PROXY_URL=http://localhost:3001

# The Flask backend directly (OAuth only — the proxy intentionally does not
# forward /auth/* routes, since the OAuth redirect must hit Flask directly)
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000
